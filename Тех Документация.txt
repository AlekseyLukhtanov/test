🤖 Что умеет этот скрипт:
📌 Обязательные возможности:
При запуске запрашивает ссылку или @username группы Telegram для сбора участников.

Запрашивает название проекта — используется как имя папки в директории Downloads, где будут сохраняться результаты.

Все результаты сохраняются в виде CSV-файлов по 50 username в каждом.

Если возникают ошибки (недостаточные права, приватность группы и т.п.) — создаётся лог errors.log в той же папке.

Поддержка асинхронного выполнения (asyncio) и обработка FloodWait и других ошибок.

Использует Telethon и config.json для конфиденциальной авторизации (api_id, api_hash, phone_number).

🛠 Этапы работы скрипта:
🧩 Этап 1 — Сбор участников:
Собирает всех участников всеми доступными способами:

GetParticipantsRequest — если есть доступ к списку участников.

Из истории сообщений (до 300 000 сообщений).

Из комментариев к постам, если они доступны.

⏱ Между запросами используется задержка в 1 секунду для защиты от FloodWait.
📊 Используется tqdm.asyncio — отображается прогресс-бар и ETA.

⚙️ Фильтрация — на выбор пользователя:
Перед фильтрацией скрипт спрашивает:

✅ Хотите ли исключить спамеров?

✅ Хотите ли фильтровать по активности (был онлайн за последние 2 дня)?

✅ Хотите ли исключить администраторов/модераторов?

🔎 Этап 2 — Поиск спамеров:
Анализируются последние до 100 000 сообщений за 40 дней.

Исключаются пользователи, которые писали одинаковые сообщения 3+ раза.

⚡ Этап 3 — Фильтрация по активности:
Включаются только активные пользователи:

Online или недавно.

Либо с последним визитом не более 2 дней назад.

Остальные — исключаются.

🛡 Этап 4 — Исключение админов:
Исключаются пользователи с админскими или особыми правами в группе.

💾 Финал:
Только пользователи с username включаются в финальный список.

Имена сохраняются в CSV-файлы по 50 штук в директории Downloads/<название проекта>.

Все ошибки фиксируются в errors.log.